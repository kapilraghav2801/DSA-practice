https://leetcode.com/problems/number-of-1-bits/
class Solution:
    def hammingWeight(self, n: int) -> int:
        count = 0
        while n != 0:
            count += 1
            n = n & (n-1)

        return count
        # count = 0
        # string = bin(n)[2:]
        # for str in string:
        #     if str == '1':
        #         count += 1
        # return count
